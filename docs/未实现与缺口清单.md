# TrustAid Platform：未实现功能与缺口全集

**文档版本**：1.2  
**仓库路径**：`trustaid-platform/`（含 `backend`、`contracts`、`circuits`、`frontend`，并交叉引用根目录与脚本）  
**编制方法**：全文检索关键字（TODO、占位、Mock、deprecated、offchain、MVP、预留、暂无、VRF、测试网等）+ `app` 路由清点 + `server.js` / `deploy.mjs` 核对 + `DashboardShell` 导航与 `**/page.tsx` 对照。  
**说明**：下列条目包含「尚未开发」「仅占位/UI 壳」「有意简化的 MVP」「环境与导航不一致导致的不可用」四类；在每类中用措辞区分。

---

## 一、路线图与自述文档中的未完成项

来源：`README.md` 「下一步计划」：

| # | 条目 | 备注（与仓库现状对照） |
|---|------|------------------------|
| 1 | 完成抗女巫电路的完整约束（Merkle 深度=8） | 电路侧已有参数化模板（如 `AntiSybilVerifier(merkleLevels)`），是否与 README 勾选一致需以部署的编译参数为准；README 仍列为未完成。 |
| 2 | 集成 Chainlink VRF 仲裁随机抽选 | 合约仍为 `block.prevrandao`，见下文合约节。 |
| 3 | 实现前端 WebWorker 证明生成 | 仓库已存在 `frontend/workers/zk.worker.ts`、`public/workers/zkWorker.js`，README 条目可能过时，但 Worker 在无产物时仍会 **Mock**。 |
| 4 | 更多单元/集成测试 | `frontend/tests/wallet.test.ts` 等仍存在占位断言。 |
| 5 | 完善 AI 安全审计 | 后端审计为规则集，见下文。 |
| 6 | 生产环境多重签名部署 | 治理/多签依赖环境变量与链上配置，未配置时为 offchain 占位。 |

---

## 二、合约层（`contracts/`）

### 2.1 明确标注为占位或测试网方案

| 位置 | 缺口类型 | 说明 |
|------|-----------|------|
| `contracts/contracts/verifiers/MockGroth16Verifier.sol` | 生产缺失 | 注释写明：生产替换为 `snarkjs` 导出的 Verifier；`scripts/deploy.mjs` 仍固定部署 Mock。 |
| `contracts/contracts/governance/ArbitratorPool.sol` | 未集成 | 伪随机 `pick`；注释要求 **Chainlink VRF**；含「仅测试用」`emergencyWithdraw`。 |
| `contracts/contracts/channels/PaymentChannel.sol` | 未实现 | `emergencyClose()` 为空壳（预留）。 |

### 2.2 部署脚本中的「非生产」假设

`contracts/scripts/deploy.mjs`：**PaymentChannel** 使用 Hardhat 固定测试地址 participant1/2；**AnonymousClaim** 使用示例 Merkle root（`example_merkle_root` 哈希）与时间窗口——属于本地/演示部署逻辑，不是可照搬生产的参数化部署。

### 2.3 电路 ↔ 合约覆盖缺口

- 仓库 `circuits/src/` 中存在 **private_payment、confidential_transfer、privacy_payment、reputation_verifier、multi_sig_proposal、history_anchor** 等多条电路。
- **默认部署流水线**（见 `deploy.mjs`）：已为 **ClaimVaultZK / AnonymousClaim 分离 Mock 验证器**，并在同次部署中登记 **其余八条电路的 Groth16Verifier** 地址；前端静态清单见 `frontend/public/circuits/build/circuits-manifest.json`，可用 `circuits` 下 `npm run sync:frontend` 同步 wasm/zkey。各业务页与强类型 witness 仍可按产品优先级逐项补齐。

---

## 三、电路（`circuits/`）

| 类别 | 说明 |
|------|------|
| **文档 TODO** | `circuits/circom-source/mkdocs/docs/...` 等上游文档内仍有 **TODO**（与运行时代码无关，属文档维护缺口）。 |
| **可信设置与产物** | `circuits/README.md`：`zkey` 涉密需 CI/私有注入；未生成 `.wasm/.zkey` 时前端只能降级（见前端节）。 |
| **Windows/测试** | README 注明 `npm test` 可跳过重编译；完整 Circom 测试需环境变量或 WSL——属于工程完备度而非功能开关。 |

---

## 四、后端（`backend/`）

### 4.1 Explorer HTTP 能力（已与实现对齐）

| 能力 | 说明 |
|------|------|
| **`/v1/explorer/*`** | **`server.js` 已挂载** `app.use("/v1/explorer", explorerRoutes)`；实现见 `backend/src/routes/explorer.routes.js`、`services/explorer.service.js`。依赖环境变量 **`RPC_URL`**（或 `config.rpcUrl`）；未配置时统计接口返回 `source: "offline"` 与零值，**非「路由缺失」**。 |

### 4.2 有意简化的 MVP / 占位逻辑

| 模块 | 文件 | 说明 |
|------|------|------|
| ZK 预校验 | `backend/src/services/zkVerify.service.js` | 主要为结构与 groth16 **MVP** 校验，非完整链下 snark 验证。 |
| AI 理赔审计 | `backend/src/services/aiAudit.service.js` | 基于规则的启发式，非文档描述的完整 LLM 语义审计 + Diff。 |
| 风险推荐 | `backend/src/services/riskEngine.service.js` | **占位**：简单启发式，注释写生产需链上指标。 |
| 治理内存模型 | `backend/src/storage.js` | `createProposal`/`castVote` 为内存存储；票数 **演示性 +10**；与链上 `Governance.sol` 不同步，除非走多签服务且配置齐全。 |
| 多签 ↔ 链上 | `backend/src/services/multiSig.service.js` | 未配置 `RPC_URL`/`relayerPrivateKey`/`GOVERNANCE_ADDRESS` 时，`queue`/`execute`/`cancel` 返回 **mode: "offchain"** 的成功语义，非真实链上交易。 |
| 匿名申领 / 身份 | `backend/src/services/anonymousClaim.service.js`、`identity.service.js` | 合约未配置时为 **offchain** 模式；身份服务含「离线降级」分支。 |
| 开发认证降级 | `backend/src/middleware/auth.js` | `BYPASS_AUTH` 等 **禁止用于生产**（注释已说明）。 |

### 4.3 已标记废弃仍未移除

| 端点 | 说明 |
|------|------|
| `POST /v1/oracle/legacy-report` | `oracle.routes.js` 标注 **@deprecated**；`docs/角色/后端路由矩阵.md` 建议迁移。 |

### 4.4 有 REST 但前端覆盖不全的域

| 域 | 现状（以代码为准） |
|----|-------------------|
| **Channel** | [`frontend/lib/api.ts`](../../frontend/lib/api.ts) 含 `listAllChannels` → `GET /v1/channel/all`；[`oracle/channels/page.tsx`](../../frontend/app/oracle/channels/page.tsx) 已使用。**开通/更新通道等**仍无面向终端用户的完整 UI 流程。 |
| **Reputation** | 后端 `reputation.routes.js` 完整；前端 [`lib/api/client.ts`](../../frontend/lib/api/client.ts) 已映射路径，**尚无独立「声誉」业务页**调用这些端点。 |
| **其它** | 与 §5.6 多签 ZK 等一致：路由存在 ≠ 产品闭环。 |

### 4.5 隐私与设计债（功能「存在」但与「Witness 不离端」准则冲突）

| 端点 | 说明 |
|------|------|
| `POST /v1/security/nullifier/derive` | `security.routes.js` 接收 **secret** 在服务端派生 Nullifier——若严格坚持「私密见证不过服务器」，生产需改为纯客户端派生或其他协议。 |

### 4.6 存储与 README

`backend/README.md`：说明示例服务默认 **内存存储**，PostgreSQL 为可选迁移——即持久化层需自行保证完整启用。

---

## 五、前端（`frontend/`）

### 5.1 工作台子路由（已从占位对齐后端）

以下路径均已实现 `app/**/page.tsx`，并与 [`frontend/lib/api.ts`](../../frontend/lib/api.ts)、后端 `/v1/challenge`、`/v1/oracle/reports`、`/v1/guardian/blacklist` 等对接（详见「工作台子页面」迭代说明）：

| 导航 href | 说明 |
|-----------|------|
| `/arbitrator/cases` | 仲裁任务列表（`GET /v1/member/arb/tasks/my`） |
| `/arbitrator/vote` | Commit/Reveal 表单（支持 `?proposalId=`） |
| `/challenger/challenges` | 挑战记录列表 |
| `/challenger/rewards` | 挑战记录 + 质押/结算说明 |
| `/oracle/reports` | 报告摘要列表与详情 |
| `/oracle/channels` | `GET /v1/channel/all` |
| `/guardian/circuit` | 熔断 + 状态 + 审计片段 |
| `/guardian/blacklist` | 黑名单表单 + 列表 |
| `/dao/treasury` | 内存治理提案摘要 + 可选链上 Governance 余额 |

链上 ChallengeManager 结算、**Indexer 独立服务**、声誉/多签完整产品页等仍属其他章节所述缺口；**Explorer REST 已与前后端对齐**（见 §4.1、§5.5）。

### 5.2 已有页面但仅为占位/空壳

| 路径 | 说明 |
|------|------|
| `/dao/proposals`、`/dao/members` | 静态「暂无」；主治理交互在 `/dao/page.tsx`。 |
| `/member/family`、`/member/benefits` | 占位文案与按钮，无业务接线。 |
| `/member/profile` | 依赖 API；无数据时展示「暂无」——需后端/链数据才有实质内容。 |

### 5.3 组件 / Worker / 数据层缺口

| 项 | 位置 | 说明 |
|----|------|------|
| 交易预检 UI | `frontend/lib/wallet/EmbeddedProvider.ts` | 已实现发送前 **`window.confirm`**（目标地址、金额、`estimateGas`、data 概要）；非阻止式专业 UI 仍可迭代。 |
| ZK Mock | `frontend/public/workers/zkWorker.js` | 缺少 `.wasm/.zkey` 时 **mockProve**，链上中继开启时后端拒绝。 |
| Explorer Mock | `frontend/lib/explorer/mock-data.ts` | 开发降级占位；主路径走 **`/v1/explorer/*`**。 |
| Explorer 统计 Hook | `frontend/hooks/useQueries.ts` **`useExplorerStats`** | 已由 [`ExplorerStats.tsx`](../../frontend/features/explorer/ExplorerStats.tsx) 使用，请求 **`GET /v1/explorer/stats`**。 |
| Indexer 默认 URL | `frontend/lib/explorer/explorer-service.ts` | `NEXT_PUBLIC_INDEXER_API_URL` 默认本地；**Compose 未带 Indexer** 时搜索/索引增强仍受限。 |
| ProofResult 文案 | `frontend/features/zk/ProofResult.tsx` | 含 **MVP** 表述（产品完成度提示）。 |
| Topbar | `frontend/features/governance/Topbar.tsx` | 「中间区域（预留）」。 |
| NavItems | `frontend/features/governance/NavItems.ts` | `@deprecated` 旧 API。 |
| Wagmi 封装 | `frontend/lib/wagmi/config.ts` | `createWagmiConfig`/`createQueryClient` **已废弃** 警告。 |

### 5.4 前端 API 映射（`client.ts`）与后端

[`frontend/lib/api/client.ts`](../../frontend/lib/api/client.ts) 中 **`channel.open` / `channel.listAll`**、**`explorer.*`**、**`multiSig.*`**、**`reputation.*`** 等已与对应 `routes/*.routes.js` **路径对齐**。

仍须注意：**映射存在 ≠ 业务页面已全部调用**；未使用的端点清单见 §4.4、§5.6。

### 5.5 区块浏览器页面数据来源

- **统计看板**：`ExplorerStats` 使用 **`useExplorerStats`** → **`GET /v1/explorer/stats`**（React Query 轮询）。
- **列表 / 搜索**：`explorer-store` 仍通过 **`lib/api/explorer`** 调后端；失败时可走 **`explorer-service`** RPC 降级——与 §4.1 **RPC 依赖**一致。
- **缺口**：无独立 Indexer 时，「全链级」列表/统计仍弱于专业浏览器；属架构限制而非单一接口缺失。

### 5.6 多签 ZK 前端

- **未找到**调用 `/v1/multisig/*` 或生成 `multi_sig_proposal.circom` 证明的页面或 hooks（仅 `client.ts` 有无用片段）。

---

## 六、基础设施与配置一致性（跨目录）

| 项 | 说明 |
|----|------|
| Docker backend healthcheck | `server.js` 提供 **`GET /health`** 极简探活；`docker-compose.yml` 使用 `curl http://localhost:3010/health`，**路径一致**。 |
| API 端口 | 请以 `frontend/.env.local.example` 与 `next.config.mjs` 代理为准；后端默认 **3010**。若本地仍写 3001，需自行统一。 |
| Compose 无 Indexer | 仅有 postgres、ipfs、backend、frontend → **无区块索引服务**配合 `explorer-service` 的 Indexer 分支。 |

---

## 七、脚本、测试与工程占位

| 文件 | 说明 |
|------|------|
| `scripts/loadtest/k6-stub.js` | k6 压测 **占位**。 |
| `frontend/tests/wallet.test.ts` | 多处 `expect(true)` **占位测试**。 |
| 后端测试 | `mockGroth16Proof` 等属测试替身，不记入「功能缺失」，但说明链上验证依赖 Mock。 |

---

## 八、`wallet-sdk/`（仓库内子包）

- `wallet-sdk/README.md` 描述 Node 端 `switchChain` 等为 **UNSUPPORTED_METHOD**——属 API 边界声明，不是 bug。
- `wallet-sdk/docs/钱包模块架构与实现详解.md` 含「降级策略」——与前端嵌入式钱包、宿主能力相关，集成时需注意。

---

## 附录 A：`frontend/app` 已实现路由一览（用于核对「缺页」）

当前存在 `page.tsx` 的路径（24 个）：

`/`，`/claim`，`/wallet`，`/explorer`，`/dao`，`/dao/proposals`，`/dao/members`，`/dao/treasury`，`/member`，`/member/profile`，`/member/family`，`/member/benefits`，`/arbitrator`，`/arbitrator/cases`，`/arbitrator/vote`，`/challenger`，`/challenger/challenges`，`/challenger/rewards`，`/oracle`，`/oracle/reports`，`/oracle/channels`，`/guardian`，`/guardian/circuit`，`/guardian/blacklist`。

其它导航 href 若不在此列表中，可视为 **未实现页面**。

---

## 附录 B：电路源文件列表（需单独做「是否接入产品」矩阵）

`circuits/src/` 下包括但不限于：`anonymous_claim.circom`、`anti_sybil_claim.circom`、`anti_sybil_verifier.circom`、`identity_commitment.circom`、`multi_sig_proposal.circom`、`private_payment.circom`、`privacy_payment.circom`、`confidential_transfer.circom`、`reputation_verifier.circom`、`history_anchor.circom` 及 `utils/*`。  
**默认部署与前端主流程未证明全部覆盖**——若需电气级全枚举，建议另表列出：编译状态、zkey、对应合约、调用页面。

---

## 已落实修复（对照表 · 维护时更新）

下列条目已在代码库中闭环；正文各节描述**长期缺口**，本表描述**已解决项**，以免重复提票。

| 原缺口 | 处理 |
|--------|------|
| 后端 `/v1/explorer/*` | `explorer.routes.js` + `explorer.service.js`，`server.js` 挂载；依赖 `RPC_URL`。 |
| Docker `/health` | `GET /health` 与 compose healthcheck 一致。 |
| `API_ENDPOINTS` / `baseURL` | `client.ts` 与 channel、multisig、reputation、explorer 对齐；默认后端端口 **3010**。 |
| 工作台导航 9 子路由 | 已实现完整子页 + `features/workbench/*`（仲裁 / 挑战 / 预言机 / 守护 / DAO 财库）。 |
| `EmbeddedProvider` 预检 | 发送前确认框（地址、金额、Gas、data）。 |
| `useExplorerStats` 未引用 | **`ExplorerStats.tsx`** 改为使用 `useExplorerStats`；与 `GET /v1/explorer/stats` 闭环，见 §5.3、§5.5。 |

**路线图级未闭环**（Mock Verifier、VRF、`PaymentChannel.emergencyClose`、治理内存与链上完全同步、Indexer 等）仍以正文 §一～§三、§5.6 为准。

---

**最后更新**：2026-04-18（v1.2：与代码核对，修正 Explorer/Channel/client/healthcheck/附录路由；ExplorerStats 接入 useExplorerStats）
